# CipherPay ZK Audit – copy to .env and fill in

# API base URL (cipherpay-server)
NEXT_PUBLIC_API_URL=http://localhost:8788

# ZK Audit server (if separate)
# ZKAUDIT_SERVER_URL=http://localhost:3001
